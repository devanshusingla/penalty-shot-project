"""Here we simply import all of our envronment classes so that they are available for use directly without needing to
import their specific python files (bandit_walk.py in this case).
"""
from gym_env.envs.penalty_shot import PSE
